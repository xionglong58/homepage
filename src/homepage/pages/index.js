import React, { Component } from 'react';
import { ListItem, ListWrapper } from '../style';
class List extends Component {
    state = {}
    render() {
        return (<ListWrapper>
            <ListItem>List</ListItem></ListWrapper>
        );
    }
}

export default List;